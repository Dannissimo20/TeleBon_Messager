import { Loader } from './CommonLoader.styled';

import { ReactComponent as Icon } from '../../icons/loader.svg';

const CommonLoader = () => {
  return (
    <Loader>
      <Icon />
    </Loader>
  );
};

export default CommonLoader;
